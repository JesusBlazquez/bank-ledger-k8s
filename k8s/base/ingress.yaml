apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ledger
spec:
  ingressClassName: nginx
  rules:
    - host: ledger.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ledger-app
                port:
                  number: 80
